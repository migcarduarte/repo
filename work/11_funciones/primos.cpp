#include <stdio.h>
#include <stdlib.h>

#define NPRIMOS 70

int main(int argc, char *argv[]) {

    void muestra(int matriz[NPRIMOS]
	    primo[0] = 2;
	    candidato = primo[0] + 1;

	    while(phe <NPRIMOS){
	    bool es_primo = true;

	    for (int p=0; p<phe; p++)
	    if ( candidato % primo[p] == 0)
	    es_primo = false;

	    if (es_primo)
	    primo[phe++] = candidato;

	    candidato++;

	    }

	    muestra(primo);


	return EXIT_SUCCESS;
}
