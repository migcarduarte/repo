#include <stdio.h>
#include <stdlib.h>

#define PALABRAS(X) ( sizeof(x) / sizeof(char *) )

int main(int argc, char *argv[]) {

    int ord[];
    int lista[];

    for (int i=0; ord[i]; i++)
	printf(
		"ave"
		"dog"
		"cocodrilo"
		"gato"
	      );

    const char *lista[]= {
	"dog",
	"ave" ,
	"cocodrilo",
        "gato",
	NULL 
    };

    for (int i=0; lista[i]; i++)
    printf("%s\n", lista[i]);

	return EXIT_SUCCESS;
}
